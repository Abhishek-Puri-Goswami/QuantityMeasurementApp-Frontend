# Local development defaults — safe to commit (no secrets here).
# Override in .env.local for personal values. Never commit .env.local.
# See .env.example for full documentation of every variable.

VITE_API_URL=http://localhost:8080
